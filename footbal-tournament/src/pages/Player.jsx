import React from "react";
import PlayerDetails from "../components/PlayerDetails";

const Player = () => {
    return (
        <div>
            <PlayerDetails/>
        </div>
    );
}

export default Player;

